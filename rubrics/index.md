# Rubrics

A rubric is one way to grade, not the only way. Load a file only when its scope matches the work. A file marked framework only is not a grading rule until its slots are filled in.

| Id | Scope | Load when | File |
| --- | --- | --- | --- |
| workflow-design | Workflow design | Designing or changing a workflow, or checking an orchestrator handoff | rubrics/workflow-design.md |
| deterministic-code | Deterministic code | The step uses deterministic code | rubrics/deterministic-code.md |
| autonomous-worker-node | Code and Jev worker node | The step is an autonomous worker node | rubrics/autonomous-worker-node.md |
| local-gpu | Local GPU | The step uses a local GPU | rubrics/local-gpu.md |
| subscription-model | Subscription model | The step uses a subscription model | rubrics/subscription-model.md |
| pay-per-use | Pay-per-use tokens | The step uses pay-per-use tokens | rubrics/pay-per-use.md |
